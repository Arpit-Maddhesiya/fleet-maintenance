export default function ServiceRecordDetailPage() {
  // Full detail view lands in Module F3; until then this is a stub so rows in
  // the list can link somewhere real.
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      Service record detail coming soon.
    </div>
  );
}
